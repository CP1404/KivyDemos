BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'vertical'
        # this layout is populated with (button) widgets in app code
        id: entries_box
        cols: 1
        orientation: 'vertical'
    Label:
        size_hint_y: 0.1
        text: app.status_text
