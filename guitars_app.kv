BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        # this layout is populated with (button) widgets in app code
        id: entries_box
        orientation: 'vertical'
    Label:
        size_hint_y: 0.1
        text: app.status_text
