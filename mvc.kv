BoxLayout:
    Label:
        text: app.message
        font_size: 30
    TextInput:
        id: user_input
        multiline: False
        # This event is when the user enters text (and presses Enter)
        on_text_validate: app.handle_update()